# RetireLab Release 6.9.1 — Accumulation Availability Fix

## Fixed
- The “Accumulation calculation is switched off” overlay now disappears correctly when Calculate accumulation is enabled.
- The overlay's CSS no longer overrides RetireLab's shared hidden class.
- The Accumulation tab immediately reflects the Dashboard switch state.
- Checkbox changes and restored project settings both refresh the tab availability.
- The switch continues to save with the active project.
