# RetireLab 6.1.2 — Help & Deployment Fix

- Adds cache-busting version tags to every CSS and JavaScript asset.
- Places each strategy explanation inside its related policy box.
- Makes the ? controls work on hover, keyboard focus and click/tap.
- Prevents stale 6.1 assets being mixed with the 6.1.1/6.1.2 index.
- Retains the 6.1.1 model-validation fixes.
